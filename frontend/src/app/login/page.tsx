import { PlaceholderPage } from "@/components/placeholder-page";

export default function LoginPage() {
  return (
    <PlaceholderPage
      title="Login"
      description="Supabase Auth (email / password) — TEACHER or STUDENT role."
    />
  );
}
