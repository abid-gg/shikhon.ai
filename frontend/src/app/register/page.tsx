import { PlaceholderPage } from "@/components/placeholder-page";

export default function RegisterPage() {
  return (
    <PlaceholderPage
      title="Register"
      description="Create an account with role metadata for the public.users profile trigger."
    />
  );
}
